Edit package files

bpm edit <file_name>

It will open all files that matches with the given file name with your >default text editor<.

If the EDITOR variable isn't defined, it will try to open with common editors, like nvim, vim or nano.
