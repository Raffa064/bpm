BPM unintall

bpm uninstall [-d]

It will uninstall >bpm executable<. If >-d< flag is specified, it will also delete the entira bpm directory (which contains all packages that have been installed from external sources, and bpm's state and configurations).
